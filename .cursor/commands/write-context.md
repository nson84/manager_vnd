Write `CONTEXT.md` for a feature module.

Ask me if scope is **BE** or **FE** (or both).

- **BE**: follow `BE/.cursor/commands/write-context.md`
  → place at `BE/src/main/java/Manager_vnd/Manager/feature/{name}/CONTEXT.md`

- **FE**: follow `FE/.cursor/commands/write-context.md`
  → place at `FE/src/features/{name}/CONTEXT.md`

If both sides of a feature, write separate CONTEXT.md in each with cross-references to BE endpoints used.
